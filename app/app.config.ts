export default defineAppConfig({
  ui: {
    button: {
      slots: {
        base: ['cursor-pointer'],
      },
    },
    navigationMenu: {
      slots: {
        link: ['cursor-pointer'],
      },
    },
  },
})
